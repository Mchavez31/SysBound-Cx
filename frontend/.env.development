# Vite dev server proxies /api → this URL (must match uvicorn --port in package.json "dev:api")
# 8020 avoids WinError 10013 on some Windows setups where 8000/8001 are reserved or blocked.
VITE_DEV_PROXY_API=http://127.0.0.1:8020
